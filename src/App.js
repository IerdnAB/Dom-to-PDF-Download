import ResignationLetter from "./components/TerminationLetter";
import "./App.css";

function App() {
  
  return (
    <div className="App" style={{ overflow: "hidden", pageBreakInside: "avoid", display: "flex" }}>
      <ResignationLetter />
    </div>
  );
}

export default App;
